import { GlobalState, GlobalStateParams } from "./types";

export function getState({  }: GlobalStateParams): GlobalState {
    return {
        store: {

        },
        actions: {

        }
    };
}